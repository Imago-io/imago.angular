input(
  type="file"
  name="receipt"
  id="receipt"
  sizeerror="sizeerror"
  )
label(for="receipt" ng-if="!filename") {{label}} (max {{maxFileSize}}mb)
label(for="receipt" ng-if="filename") File: {{filename}}

.error(ng-show="sizeerror") Image File to large